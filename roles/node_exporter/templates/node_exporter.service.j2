[Unit]
Description=Node Exporter
After=network.target

[Service]
User=node_exporter
Group=node_exporter
Type=simple
ExecStart= {{ node_exporter_path }}/node_exporter --collector.systemd

[Install]
WantedBy=multi-user.target